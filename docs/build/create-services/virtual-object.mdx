---
title: Virtual Object
description: Create stateful objects with persistent state and consistency guarantees
---



## Build objects with persistent, consistent state

Virtual Objects maintain state tied to a unique key, providing consistency guarantees and single-writer semantics. Each object instance can store and modify data while ensuring concurrent operations don't conflict.

Use virtual objects for user accounts, shopping carts, game state, or any entity that needs reliable state management with strong consistency.