---
title: Store & Protect State
description: Combine ctx.get/set with conflict-prevention tips
---

## Safe state management patterns

Learn to effectively use `ctx.get()` and `ctx.set()` for state management while leveraging Restate's single-writer guarantees to prevent conflicts. Understand patterns for atomic updates and state consistency.

These techniques ensure your stateful services maintain data integrity even under concurrent access patterns.