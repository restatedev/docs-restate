---
title: Persist Side-effects
description: Wrap HTTP/DB calls with ctx.run, deterministic randoms
---

## Safe external operations

Wrap HTTP calls, database operations, and other side-effects with `ctx.run()` to ensure they're executed exactly once. Learn patterns for deterministic random generation and handling external system integration.

These techniques ensure your services remain consistent and predictable even when interacting with external systems.