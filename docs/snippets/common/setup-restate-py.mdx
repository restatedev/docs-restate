[Install Restate](/installation) and launch it:
```bash
restate-server
```

Get the example:
```bash
git clone https://github.com/restatedev/ai-examples.git && cd ai-examples/python-restate-only/tour-of-agents
```

Export your API key:
```bash
export OPENAI_API_KEY=sk-...
```