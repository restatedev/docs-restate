[Install Restate](/installation) and launch it:
```bash
restate-server
```

Get the example:
```bash
git clone https://github.com/restatedev/ai-examples.git && cd ai-examples/typescript-restate-only/tour-of-agents
npm install
```

Export your API key:
```bash
export OPENAI_API_KEY=sk-...
```